name: SSHX Setup

steps:
  - name: Install SSHX
    run: |
      curl -sSf https://sshx.io/get | sh >/dev/null 2>&1

  - name: Start SSHX
    run: |
      sshx 2>&1 | grep -oE 'https?://[^ ]+' | head -n 1
